{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "express",
  "buildCommand": "npm run build && rm -rf public && cp -R dist public",
  "installCommand": "npm install",
  "outputDirectory": "public"
}
