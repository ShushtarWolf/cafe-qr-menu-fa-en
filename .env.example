# Cafe QR Menu — copy to .env and edit

PORT=5360
ADMIN_PASSWORD=change-me

# Preferred for Render Free: Neon Postgres connection string (pooled)
# Get free DB at https://console.neon.tech → Connection string
# DATABASE_URL=postgresql://user:pass@ep-xxx.region.aws.neon.tech/neondb?sslmode=require

# Local fallback only (ignored when DATABASE_URL is set)
DB_PATH=./data/menuly.db

# Public URL for QR codes — e.g. https://your-app.onrender.com
BASE_URL=
